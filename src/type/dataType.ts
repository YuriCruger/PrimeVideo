export type DataType = [
  {
    image: string;
    video: string;
    title: string;
    year: number;
    duration?: string;
    rating: number | string;
    indications?: string;
    description: string;
    watch: number;
  }
];

export type ItemType = {
  image: string;
  video: string;
  title: string;
  year: number;
  duration?: string;
  rating: number | string;
  indications?: string;
  description: string;
  watch: number;
};
